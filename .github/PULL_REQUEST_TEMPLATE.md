## What & why

What this changes, and the reason for it.

## How it was tested

The `make` target or script run, and how you confirmed the affected service is
healthy (e.g. `docker ps`, dashboard at http://localhost:3000).

## Checklist

- [ ] No secrets committed — only `*.example` templates; real values stay in
      gitignored `.env`/generated files.
- [ ] Docker images pinned by digest.
- [ ] Docs/comments follow the `documentation-minimalism` skill.
- [ ] Updated `.claude/MEMORY.md` if the
      change is durable or architectural.
